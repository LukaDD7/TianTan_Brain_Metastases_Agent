# TianTan Brain Metastases Agent - 项目依赖
# 安装方式：pip install -r requirements.txt

# =============================================================================
# 核心依赖
# =============================================================================

# Pydantic v2 - 数据验证和设置管理
pydantic>=2.0

# HTTP 请求库（用于 API 调用）
requests>=2.28.0

# YAML 解析（用于 SKILL.md 元数据加载）
pyyaml>=6.0

# =============================================================================
# 可选依赖 - 外部 API 集成
# =============================================================================

# BioPython - 用于 PubMed XML 解析
biopython>=1.79

# =============================================================================
# 开发依赖
# =============================================================================

# 测试框架
pytest>=7.0.0
pytest-cov>=4.0.0

# 代码格式化工具
black>=23.0.0
isort>=5.12.0
flake8>=6.0.0

# 类型检查
mypy>=1.0.0
